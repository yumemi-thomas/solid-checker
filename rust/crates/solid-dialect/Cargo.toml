[package]
name = "solid-dialect"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
# Only for the negative table's citation test: an `AuditedCitation::Implementation`
# pins a runtime file's digest and its cited slice's digest, and both are
# re-derived from bytes rather than trusted.
sha2.workspace = true
