[package]
name = "solid-v1-compiler"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
solid1-dom-expressions-compiler.workspace = true
solid-facts = { path = "../../../crates/solid-facts" }

[dev-dependencies]
# The 2.0 compiler, for the test that pins the two compilers apart: one
# source, two lowerings, two different execution maps.
dom-expressions-compiler.workspace = true
