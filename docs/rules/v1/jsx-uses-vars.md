# v1/jsx-uses-vars

`SC8006` · **error** · violation

Accounts for component identifiers referenced by JSX. TypeScript reference
facts already model these uses, so this rule emits no standalone diagnostic.
