# Isolated graph compiler contexts

The captured Corvu graph demonstrates that a shared TypeScript program can
resolve one installed package copy's implementation to another copy with the
same package name and version. Materialization preserves both installations,
but the compiler's package redirect removes the exact own-source premise.
Accepting the foreign declaration would violate the certification boundary.

Keep successful shared acquisitions. When a request is locally open and the
authenticated graph positively contains another installation of that request's
package name/version, reacquire the entire request in a fresh pinned session.
This is a bounded additional acquisition, not a relaxed verification verdict.
Infrastructure, identity-envelope and source-census errors are not retry hints.

The isolated project preserves every authenticated installed directory and its
relative dependency topology. Only compiler roots and harness subjects narrow
to the requested plan. Schedule derivation retains all implementation owners;
verification retains the same dependency authority and source census. Shared
and isolated acquisition call the same verifier sequence, including local
census acquisition. No transcript or receipt crosses between those sessions.
If the isolated request still cannot prove its claims, it remains refused.

This changes internal acquisition only. Public protocols, receipt fields,
trust policy, contract claims and coverage denominators remain unchanged.
Focused native tests must establish the shared original-input refusal, successful
certification under two distinct importer bindings, and refusal after an input
is mutated in one installation. Package coverage requires separate measurement
through final publication and ordinary consumer verification.

Focused validation passes two armed native tests (4.71 seconds). The chunk
fixture proves the original shared family refusal and successful certification
under both importer bindings. Different runtime bytes at one installation
refuse at the source census; that mutated case queried alone refuses its own
original-input identity. Retry classification also rejects source-census,
producer-provenance, missing-demand, unsupported-demand and envelope-identity
errors.

Full `make verify` completed with actual exit 0, TOTAL 123.30 seconds and no
`FAILED during step` marker. No existing snapshot changed.

The fresh Corvu diagnostic finishes in 16.489 seconds with an expected refusal
(exit 2) and no cache misses. It advances past `@corvu/utils:contains` to
`@floating-ui/utils@0.2.12 ./dom:getOverflowAncestors`, whose parameter-rooted
read still lacks positive original-input identity. The row remains refused,
with zero accepted cases before and after; the
[measurement](../package-contract-v2/phase21/2026-09-08-corvu-isolated-context-measurement.json)
claims no new certification or denominator correction. The full corpus has not
been rerun after this acquisition change.
