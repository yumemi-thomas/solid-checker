# Complete imported union premises: measured shortfall

Protocol 38 retains **462 withheld candidates**: **442 creates** (405 census
refusals and 37 veto failures) and **20 returns**. The complete 418-probe run
matches the preceding installed versions and row statuses: 368 certified,
30 refused and 20 probes not advanced. No candidate identities were added or
removed; no complete candidate closed.

Eighteen occurrence-level explanations changed, six per motion-dom export:

| Export | Before | After | Candidate gain |
| --- | --- | --- | --- |
| buildHTMLStyles | TransformTemplate optional helper premise became any | Premise binds; template coercion at build-transform.mjs:1755..1790 remains | 0 |
| removeAxisTransforms | Optional Axis helper premise became any; binary coercion at delta-remove.mjs:956..989 | Premise binds; later binary coercion at 1162..1186 | 0 |
| calcBoxDelta | Optional AnyResolvedKeyframe helper premise became any | Resolves to string \| number \| undefined, but printed text differs from the demanded alias | 0 |

This confirms complete union spelling fires in the corpus. It does not
establish the remaining template operands or later arithmetic operands as
primitive. The alias mismatch also remains an explicit producer limitation:
do not drop printed-type equality merely because outer flags agree.

The [evidence JSON](2026-09-07-union-census-measurement.json) preserves every
candidate occurrence, audit hashes, exact changed reasons and executable
identities. First-refusal categories are unchanged from protocol 37.

The producer regression failed before the fix and passed afterwards. It
confirms an actually recorded coercion, successful optional import transport,
and refusal of null substitution and a same-named type from another module.
The packed native fixture closes numeric Axis and refuses unknown members.
Full **make verify passed in 210.39 seconds**, exit 0, TOTAL present and no
failure marker. No snapshots or public package contracts changed in this
slice. Nothing was committed or pushed. The overall goal remains unfinished.
