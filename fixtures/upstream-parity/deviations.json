{
  "schemaVersion": 2,
  "fired": {
    "components-return-once__invalid__00": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noEarlyReturn)."
    },
    "components-return-once__invalid__01": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noEarlyReturn)."
    },
    "components-return-once__invalid__02": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noEarlyReturn)."
    },
    "components-return-once__invalid__03": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noConditionalReturn)."
    },
    "components-return-once__invalid__04": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noConditionalReturn)."
    },
    "components-return-once__invalid__05": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noConditionalReturn)."
    },
    "components-return-once__invalid__09": {
      "status": "evidence-backed",
      "reason": "The checker reports a conditional return only when the condition is proven reactive (SC1004); upstream reports the shape regardless of what the condition reads (noEarlyReturn)."
    },
    "jsx-no-undef__invalid__00": {
      "status": "fact-unavailable",
      "reason": "Upstream's own scope walk decides a plain JSX tag is undefined; this checker asks TypeScript, and a span with no entity is missing evidence rather than proof that no binding exists. The rule therefore fails closed on an unresolved tag and stays silent. `use:` directive names still report, because the structural binder returns an explicit negative there rather than an absence."
    },
    "jsx-no-undef__invalid__05": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__06": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__07": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__08": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__09": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__10": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__11": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__12": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__13": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__14": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "jsx-no-undef__invalid__15": {
      "status": "policy",
      "reason": "Upstream treats a bare control-flow tag as a forgotten `solid-js` import: it reports `autoImport` and splices the name in, keyed off a hard-coded Show/For/Index/Switch/Match list. That is a name-only allowlist, which this checker does not carry -- an unresolved tag is uncertifiable whatever it is spelled, so no report and no fix. The upstream fix remains covered as formatting logic by the rule's own tests."
    },
    "no-destructure__valid__10": {
      "status": "evidence-backed",
      "reason": "Upstream's own comment says this is caught by solid/reactivity rather than by no-destructure; the checker reports it from the destructure rule instead of a second one."
    },
    "reactivity__invalid__13": {
      "status": "evidence-backed",
      "reason": "This is upstream's analysis-integrity warning for a capture shape its syntax analyzer cannot follow, not a Solid runtime defect. The checker resolves sources through TypeScript symbols and does not expose analyzer bookkeeping as a user rule."
    },
    "reactivity__invalid__14": {
      "status": "evidence-backed",
      "reason": "Keeping only a signal setter is valid Solid when a scope writes but never reads the value. Upstream reports the partial tuple because its syntax analyzer expects both slots; the checker follows the resolved setter contract."
    },
    "reactivity__invalid__15": {
      "status": "evidence-backed",
      "reason": "As reactivity__invalid__13: this is an upstream analyzer-capture warning for a discarded createMemo result, not evidence of an incorrect reactive read/write contract."
    },
    "reactivity__invalid__16": {
      "status": "evidence-backed",
      "reason": "Solid lowers a function child to insert(el, fn), which calls it reactively, so <div>{signal}</div> works and upstream's report is a false positive."
    },
    "reactivity__invalid__17": {
      "status": "evidence-backed",
      "reason": "Same as reactivity__invalid__16: a memo passed as a JSX child is called by insert()."
    },
    "reactivity__invalid__25": {
      "status": "fact-unavailable",
      "reason": "SomeContext is undeclared in this corpus case, so neither TypeScript nor the compiler can prove a Context.Provider contract. The checker does not infer framework semantics from a property spelled Provider."
    },
    "reactivity__invalid__26": {
      "status": "fact-unavailable",
      "reason": "SomeProvider is undeclared and a component name ending in Provider is not proof that its value prop is eagerly consumed. The checker requires a resolved component or contract rather than upstream's name convention."
    },
    "reactivity__invalid__27": {
      "status": "fact-unavailable",
      "reason": "As reactivity__invalid__25: the corpus leaves SomeContext undeclared, so no provider contract can be resolved for either value or someOtherProp."
    },
    "reactivity__invalid__32": {
      "status": "fact-unavailable",
      "reason": "A tagged template hands its interpolations to the tag, which may legitimately call them, so uncalled-accessor deliberately excludes them; see its value_position doc."
    },
    "reactivity__invalid__33": {
      "status": "fact-unavailable",
      "reason": "As reactivity__invalid__32."
    },
    "reactivity__invalid__34": {
      "status": "evidence-backed",
      "reason": "Solid 1.x mapArray only allocates and returns an adapter; its list and mapper execute when that returned function is invoked. Here createCustomStore is never called, so canonical-symbol reachability and the AST call graph prove the mapper's store read is dormant."
    },
    "reactivity__invalid__35": {
      "status": "evidence-backed",
      "reason": "Solid 1.x mapArray does not execute its mapper until the returned adapter is invoked. The adapter is assigned to result and never consumed, so the mapper's index-accessor read is unreachable and upstream's eager syntax report is a false positive."
    },
    "reactivity__invalid__36": {
      "status": "evidence-backed",
      "reason": "Solid 1.x indexArray does not execute its mapper until the returned adapter is invoked. The adapter is assigned to result and never consumed, so the mapper's item-accessor read is unreachable and upstream's eager syntax report is a false positive."
    },
    "reactivity__invalid__37": {
      "status": "evidence-backed",
      "reason": "Solid's compiler classifies a dynamic component property as CallerContext and lowers it to a lazy getter, so signal() is evaluated when the child reads the prop rather than once at the parent call site. Upstream's report contradicts that compiler fact."
    },
    "reactivity__invalid__38": {
      "status": "fact-unavailable",
      "reason": "useExample is an unresolved ambient callee. Upstream guesses reactive behavior from its use-prefixed name; the checker requires a body or package contract and does not claim the value argument is wrong without one."
    },
    "reactivity__invalid__39": {
      "status": "fact-unavailable",
      "reason": "As reactivity__invalid__38, inside an array literal."
    },
    "reactivity__invalid__40": {
      "status": "fact-unavailable",
      "reason": "As reactivity__invalid__38, inside an object literal."
    },
    "reactivity__invalid__41": {
      "status": "fact-unavailable",
      "reason": "As reactivity__invalid__38: the IIFE is evaluated, but whether passing its value is a defect depends on the unresolved useExample contract. A use-prefixed name is not a callback/value contract."
    },
    "reactivity__valid__23": {
      "status": "evidence-backed",
      "reason": "Solid 1.9's runWithOwner saves Listener and sets it to null while invoking the callback. Capturing the current owner inside an effect restores ownership, not dependency tracking, so the signal read is genuinely untracked and the checker intentionally reports it."
    },
    "reactivity__valid__24": {
      "status": "evidence-backed",
      "reason": "runWithOwner(undefined, callback) clears Listener while the callback runs in no supplied owner. The checker follows the Solid 1.9 runtime and reports the signal read; upstream's valid case is a false negative."
    },
    "reactivity__valid__51": {
      "status": "fact-unavailable",
      "reason": "useSignal is unresolved in this corpus case. The checker does not infer a snapshot or tracking contract from use-prefixed spelling; an intentional initial props snapshot can be made explicit with untrack, or an imported helper can provide a package contract."
    },
    "reactivity__valid__52": {
      "status": "fact-unavailable",
      "reason": "As reactivity__valid__51."
    },
    "reactivity__valid__73": {
      "status": "policy",
      "reason": "strict-read-untracked requires one-time component-prop snapshots to be explicit with untrack. The fact that the local is currently unused does not change the execution contract or make the implicit snapshot certifiable."
    },
    "no-react-specific-props__invalid__00": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__01": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__02": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__03": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__05": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__06": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__07": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: `className`, `htmlFor`, and `key` do not exist on `JSX.IntrinsicElements`, so on a DOM element each is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'className' does not exist on type 'HTMLAttributes<HTMLDivElement>'\") and reporting it again duplicated the type checker. The rule now speaks only for a component, whose props may legitimately admit the key -- upstream's own cases 04 and 08 -- where no type answers the question. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-react-specific-props__invalid__09": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule, and the whole `key` arm is gone: it was already gated to lowercase-led elements, where `key` is TS2322 against the real solid-js@1.9.14 typings. Nothing is left for the checker to add on a DOM element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__00": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2561 -- `csstype`'s `CSSProperties` reports `fontSize` with the kebab-case suggestion the rule offered. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__01": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2561 -- `backgroundColor`, same shape as case 00. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__03": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2561 -- `COLOR` reports with \"Did you mean to write 'color'?\". The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__04": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2353 -- `unknownStyleProp` does not exist in `CSSProperties`, and there is no kebab form either. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__05": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2322 on the attribute -- a configured extra style prop (`css`) is not declared on `HTMLAttributes`, so the whole attribute is rejected before its keys matter. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__06": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2322 on the attribute -- same as case 05 with `styleProps` replacing the default. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__12": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2322 -- `'font-size': 10` is not assignable to `FontSize<0 | (string & {})>`; a unitless number for a length is a type error. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "style-prop__invalid__13": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element TypeScript already answers for every style-object key its `CSSProperties` describes, and its excess-property check has the same subject this rule inspects -- a fresh object literal written in place. Against the real solid-js@1.9.14 typings: TS2322 -- `'margin-top': -10` against `MarginTop<…>`, same shape as case 12. The rule still speaks where no type answers: a `-`-prefixed key, which the `[key: `-${string}`]` index signature absorbs whatever it is spelled (upstream's own case 02, which still fires), and any key on a component, whose props are whatever it declares. The string-value arms are untouched on every element. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "event-handlers__invalid__00": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'only' does not exist on type 'HTMLAttributes<HTMLDivElement>'\" -- the boolean shorthand form. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__01": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'only' does not exist\" -- same name with a function value. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__03": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onClIcK' does not exist ... Did you mean 'onClick'?\" -- a mis-cased spelling is not declared. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__04": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'oncLICK' does not exist ... Did you mean 'onclick'?\". What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__05": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onLy' does not exist\" -- boolean shorthand. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__06": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onLy' does not exist\" -- string value. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__07": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onLy' does not exist\" -- numeric value. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__08": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onLy' does not exist\" -- braced string value. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__09": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onLy' does not exist\" -- a proven-static string local. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__10": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'onDoubleClick' does not exist ... Did you mean 'onDblClick'?\" -- the non-standard name is not declared under any casing. What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "event-handlers__invalid__11": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule. Solid 1.x declares every standard handler under both its camelCase and its all-lowercase spelling and `HTMLAttributes` has no `on*` index signature, so on a standard element TypeScript answers this case against the real solid-js@1.9.14 typings: TS2322 \"Property 'ondoubleclick' does not exist ... Did you mean 'ondblclick'?\". What survives: the readability rename for a *declared* spelling (`onclick`, `ondblclick` -- case 02 and case 12 still fire), every arm on a hyphenated tag (TS2339 against stock typings, so a project using one has declared it, commonly permissively, and TypeScript is then silent), and `warnOnSpread` (cases 13-15), which type checks while Solid does not attach a spread-carried listener. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "jsx-no-duplicate-props__invalid__00": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: when both occurrences are spelled *identically*, TypeScript already makes this exact claim, and which diagnostic it makes depends on where the two live. Verified against the real solid-js@1.9.14 typings: two attributes are TS17001 \"JSX elements cannot have multiple attributes with the same name\". What survives is the case this rule exists for -- two *differently spelled* props the DOM lowering folds into one slot (`onClick`/`onclick` both become the delegated `el.$$click` write; `attr:title`/`title` share the static template attribute slot), where TypeScript sees two distinct legal properties -- plus the two identical-name orders it leaves alone: a spread followed by an attribute (case 02, which still fires) and two different spread objects. Pinned by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "jsx-no-duplicate-props__invalid__01": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: when both occurrences are spelled *identically*, TypeScript already makes this exact claim, and which diagnostic it makes depends on where the two live. Verified against the real solid-js@1.9.14 typings: an attribute followed by a spread is TS2783 \"'a' is specified more than once, so this usage will be overwritten\" -- the same sentence this rule wrote. What survives is the case this rule exists for -- two *differently spelled* props the DOM lowering folds into one slot (`onClick`/`onclick` both become the delegated `el.$$click` write; `attr:title`/`title` share the static template attribute slot), where TypeScript sees two distinct legal properties -- plus the two identical-name orders it leaves alone: a spread followed by an attribute (case 02, which still fires) and two different spread objects. Pinned by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "jsx-no-duplicate-props__invalid__03": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: when both occurrences are spelled *identically*, TypeScript already makes this exact claim, and which diagnostic it makes depends on where the two live. Verified against the real solid-js@1.9.14 typings: same as case 01; quoting the spread's key does not change the property name, so TS2783 still applies. What survives is the case this rule exists for -- two *differently spelled* props the DOM lowering folds into one slot (`onClick`/`onclick` both become the delegated `el.$$click` write; `attr:title`/`title` share the static template attribute slot), where TypeScript sees two distinct legal properties -- plus the two identical-name orders it leaves alone: a spread followed by an attribute (case 02, which still fires) and two different spread objects. Pinned by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "jsx-no-duplicate-props__invalid__04": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: when both occurrences are spelled *identically*, TypeScript already makes this exact claim, and which diagnostic it makes depends on where the two live. Verified against the real solid-js@1.9.14 typings: two attributes, TS17001, as case 00. What survives is the case this rule exists for -- two *differently spelled* props the DOM lowering folds into one slot (`onClick`/`onclick` both become the delegated `el.$$click` write; `attr:title`/`title` share the static template attribute slot), where TypeScript sees two distinct legal properties -- plus the two identical-name orders it leaves alone: a spread followed by an attribute (case 02, which still fires) and two different spread objects. Pinned by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "jsx-no-duplicate-props__invalid__05": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: when both occurrences are spelled *identically*, TypeScript already makes this exact claim, and which diagnostic it makes depends on where the two live. Verified against the real solid-js@1.9.14 typings: an attribute followed by a spread, TS2783, as case 01. What survives is the case this rule exists for -- two *differently spelled* props the DOM lowering folds into one slot (`onClick`/`onclick` both become the delegated `el.$$click` write; `attr:title`/`title` share the static template attribute slot), where TypeScript sees two distinct legal properties -- plus the two identical-name orders it leaves alone: a spread followed by an attribute (case 02, which still fires) and two different spread objects. Pinned by fixtures/tsc-oracle/rule-cases.json and fixtures/reactive-ir/eslint-compat."
    },
    "no-unknown-namespaces__invalid__00": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element every namespaced prop this rule objects to is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'model:value' does not exist on type 'HTMLAttributes<HTMLDivElement>'\"). Solid resolves its namespaces through mapped types over user-augmentable interfaces plus individually declared `on:*` events, so an unrecognised prefix has nothing to land on. That covers the `style:`/`class:` steer too: neither prefix is declared at all, so `<div class:mt-10={true} />` is a type error regardless of the style preference the rule was expressing -- a genuine gap in Solid's published typings, since the 1.x compiler supports both, but the type error already speaks at that exact span. The rule survives on components, whose props are a plain object: upstream's own cases 06 and 07 still fire, and the claim there -- the compiler special-cases namespaces only on DOM elements it lowers directly, so the prop arrives inert -- is one no type makes. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-unknown-namespaces__invalid__01": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element every namespaced prop this rule objects to is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'model:value' does not exist on type 'HTMLAttributes<HTMLDivElement>'\"). Solid resolves its namespaces through mapped types over user-augmentable interfaces plus individually declared `on:*` events, so an unrecognised prefix has nothing to land on. That covers the `style:`/`class:` steer too: neither prefix is declared at all, so `<div class:mt-10={true} />` is a type error regardless of the style preference the rule was expressing -- a genuine gap in Solid's published typings, since the 1.x compiler supports both, but the type error already speaks at that exact span. The rule survives on components, whose props are a plain object: upstream's own cases 06 and 07 still fire, and the claim there -- the compiler special-cases namespaces only on DOM elements it lowers directly, so the prop arrives inert -- is one no type makes. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-unknown-namespaces__invalid__02": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element every namespaced prop this rule objects to is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'model:value' does not exist on type 'HTMLAttributes<HTMLDivElement>'\"). Solid resolves its namespaces through mapped types over user-augmentable interfaces plus individually declared `on:*` events, so an unrecognised prefix has nothing to land on. That covers the `style:`/`class:` steer too: neither prefix is declared at all, so `<div class:mt-10={true} />` is a type error regardless of the style preference the rule was expressing -- a genuine gap in Solid's published typings, since the 1.x compiler supports both, but the type error already speaks at that exact span. The rule survives on components, whose props are a plain object: upstream's own cases 06 and 07 still fire, and the claim there -- the compiler special-cases namespaces only on DOM elements it lowers directly, so the prop arrives inert -- is one no type makes. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "no-unknown-namespaces__invalid__03": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: on an intrinsic element every namespaced prop this rule objects to is already TS2322 against the real solid-js@1.9.14 typings (\"Property 'model:value' does not exist on type 'HTMLAttributes<HTMLDivElement>'\"). Solid resolves its namespaces through mapped types over user-augmentable interfaces plus individually declared `on:*` events, so an unrecognised prefix has nothing to land on. That covers the `style:`/`class:` steer too: neither prefix is declared at all, so `<div class:mt-10={true} />` is a type error regardless of the style preference the rule was expressing -- a genuine gap in Solid's published typings, since the 1.x compiler supports both, but the type error already speaks at that exact span. The rule survives on components, whose props are a plain object: upstream's own cases 06 and 07 still fire, and the claim there -- the compiler special-cases namespaces only on DOM elements it lowers directly, so the prop arrives inert -- is one no type makes. Pinned in both directions by fixtures/tsc-oracle/rule-cases.json."
    },
    "reactivity__invalid__18": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: the native-JSX-attribute position is TS2322 against the real solid-js@1.9.14 typings (\"Type 'Accessor<unknown>' is not assignable to type '\\\"submit\\\" | \\\"reset\\\" | \\\"button\\\" | \\\"menu\\\" | undefined'\") -- an accessor is never assignable to a DOM attribute's type, in either dialect. The rule survives on the positions where an uncalled accessor is *legal* to TypeScript and silently wrong at runtime: a string-concatenation binary operand (`\"hello \" + label`, which renders the accessor's own source text), a unary operand, and a template-literal interpolation. Pinned by fixtures/reactive-ir/uncalled-accessor-v2 and fixtures/tsc-oracle/rule-cases.json."
    },
    "reactivity__invalid__22": {
      "status": "typescript-owned",
      "reason": "Narrowed under AGENTS.md's absolute rule: the computed-property-access position is TS2538 against the real solid-js@1.9.14 typings (\"Type 'Accessor<number>' cannot be used as an index type\"), in either dialect. The rule survives on the positions where an uncalled accessor is *legal* to TypeScript and silently wrong at runtime: a string-concatenation binary operand (`\"hello \" + label`, which renders the accessor's own source text), a unary operand, and a template-literal interpolation. Pinned by fixtures/reactive-ir/uncalled-accessor-v2 and fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__00": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__01": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__02": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__03": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__04": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__05": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    },
    "imports__invalid__06": {
      "status": "typescript-owned",
      "reason": "Removed under AGENTS.md's absolute rule. SC8002 fired on exactly one condition -- the module named in the import does not export the name -- and that is exactly TS2305's condition: \"Module '\\\"solid-js/web\\\"' has no exported member 'createEffect'\", in both a strict and a non-strict pass, for value and type positions alike (verified against the real solid-js@1.9.14 typings). There was no second arm: a name exported by *both* modules already returned early, so the style preference upstream expresses for `import { Show } from \"solid-js/web\"` was never reported here. Offering a module-rewrite autofix is explicitly not an exception to the rule. Pinned by fixtures/tsc-oracle/rule-cases.json."
    }
  },
  "counts": {
    "components-return-once__invalid__06": {
      "ours": 2,
      "theirs": 1,
      "status": "per-site",
      "reason": "A chained ternary: `props.cond1 ? <div/> : Boolean(props.cond2) ? <div/> : <div/>`. Both conditionals have their own reactive test, and each independently freezes its branch choice at setup, so the checker anchors one SC1004 on each proven-reactive condition (line 2 `props.cond1`, line 4 `Boolean(props.cond2)`). Upstream reports the return once, whichever way the chain nests."
    },
    "reactivity__invalid__10": {
      "ours": 2,
      "theirs": 1,
      "status": "per-site",
      "reason": "Two nested derived functions: `d` calls `e`, `e` reads `signal()`, and every call site of both is untracked. The checker names the derivation that will not update, and there are two of them, so `d` and `e` each get an SC1006. Upstream anchors its one report on the reactive read at the bottom of the chain."
    },
    "reactivity__invalid__23": {
      "ours": 2,
      "theirs": 1,
      "status": "rule-split",
      "reason": "Upstream's single `expectedFunctionGotExpression` report covers a component prop read installed directly as an event listener; the checker preserves both semantic consequences. SC1001 (v1/strict-read-untracked) reports that `props.onClick` is read outside tracking, while SC1007 (v1/expected-function-got-expression) reports that DOM setup installs only that current value. Both rules map to upstream's one `reactivity` rule, so both count against its expected 1."
    },
    "reactivity__invalid__30": {
      "ours": 2,
      "theirs": 1,
      "status": "rule-split",
      "reason": "Upstream's single `noAsyncTrackedScope` covers both halves of an async effect; the checker splits them. SC5004 (v1/no-async-tracked-scope) reports the scope -- this createEffect callback is async, so tracking stops at the first await -- and SC1002 (v1/reactive-read-after-await) reports the specific read of `count` that lands after it. Both rules are in the set upstream's one `reactivity` rule maps onto, so both count against its expected 1."
    }
  },
  "outputs": {
    "no-react-deps__invalid__00": {
      "ours": "createEffect(() => {\n        console.log(signal());\n      });",
      "status": "tighter-cleanup",
      "reason": "One shared edit across no-react-deps__invalid__00, 01, 03, 04 and 05: the checker's deletion swallows the `,` that separated the dependency array from the callback (deletion_with_leading_comma), leaving `createEffect(fn)`; upstream removes only the argument's own range and leaves `createEffect(fn, )` behind. A trailing comma in an argument list has been legal since ES2017, so both texts are the same program -- ours is just the one a reader would have written."
    },
    "no-react-deps__invalid__01": {
      "ours": "createEffect(() => {\n        console.log(signal());\n      });",
      "status": "tighter-cleanup",
      "reason": "One shared edit across no-react-deps__invalid__00, 01, 03, 04 and 05: the checker's deletion swallows the `,` that separated the dependency array from the callback (deletion_with_leading_comma), leaving `createEffect(fn)`; upstream removes only the argument's own range and leaves `createEffect(fn, )` behind. A trailing comma in an argument list has been legal since ES2017, so both texts are the same program -- ours is just the one a reader would have written."
    },
    "no-react-deps__invalid__03": {
      "ours": "const value = createMemo(() => computeExpensiveValue(a(), b()));",
      "status": "tighter-cleanup",
      "reason": "One shared edit across no-react-deps__invalid__00, 01, 03, 04 and 05: the checker's deletion swallows the `,` that separated the dependency array from the callback (deletion_with_leading_comma), leaving `createEffect(fn)`; upstream removes only the argument's own range and leaves `createEffect(fn, )` behind. A trailing comma in an argument list has been legal since ES2017, so both texts are the same program -- ours is just the one a reader would have written."
    },
    "no-react-deps__invalid__04": {
      "ours": "const value = createMemo(() => computeExpensiveValue(a(), b()));",
      "status": "tighter-cleanup",
      "reason": "One shared edit across no-react-deps__invalid__00, 01, 03, 04 and 05: the checker's deletion swallows the `,` that separated the dependency array from the callback (deletion_with_leading_comma), leaving `createEffect(fn)`; upstream removes only the argument's own range and leaves `createEffect(fn, )` behind. A trailing comma in an argument list has been legal since ES2017, so both texts are the same program -- ours is just the one a reader would have written."
    },
    "no-react-deps__invalid__05": {
      "ours": "const value = createMemo(() => computeExpensiveValue(a(), b()));",
      "status": "tighter-cleanup",
      "reason": "One shared edit across no-react-deps__invalid__00, 01, 03, 04 and 05: the checker's deletion swallows the `,` that separated the dependency array from the callback (deletion_with_leading_comma), leaving `createEffect(fn)`; upstream removes only the argument's own range and leaves `createEffect(fn, )` behind. A trailing comma in an argument list has been legal since ES2017, so both texts are the same program -- ours is just the one a reader would have written."
    }
  }
}
