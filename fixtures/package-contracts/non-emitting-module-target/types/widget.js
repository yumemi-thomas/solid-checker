// A real module reached through the same wildcard key as the two non-emitting
// members beside it. It must certify exactly as an explicit entrypoint would,
// which is what makes the two dispositions a census decision rather than a
// suppression.
export function render(callback) {
  callback();
}
