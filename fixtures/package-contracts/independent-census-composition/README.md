# Independent creates evidence still needs dependency receipts and the veto

The native test packages these exact JS/declaration bytes into two authenticated
archives. The root imports the leaf but its local typeof operation does not rely
on the leaf's semantic claims. Its independent creates census can compose with
the leaf's open receipt; the parent claim ID is not present in that receipt.

The observe recipe must complete before closure. The contradiction recipe is
an intentional harness control that emits the forbidden event and must veto
closure, even with the same complete census. This is not a real package defect.
The unsafe sibling calls an opaque imported function and must still refuse its
census. Existing receipt-only and transplanted-evidence tests remain negative.
These files are native certification inputs, not generated-main snapshots.
