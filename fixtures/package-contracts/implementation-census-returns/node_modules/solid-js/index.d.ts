// Dialect-selection stub only. The package under test calls no Solid
// primitive, so there is no signature here to keep faithful; the stub exists
// because dialect selection follows the nearest `node_modules/solid-js`
// (`rust/crates/solid-facts-backend/src/dialect.rs`) and this fixture is a
// Solid 2.0 consumer.
export {};
