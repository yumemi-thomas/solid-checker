# Factory return composition

The native published-graph test authenticates synthetic archives containing
these exact files. The root's declaration intentionally states `any`, matching
the missing root premise in Seroval Plugins 1.5.6; it grants no shape evidence.
The leaf's exact generic signature and closed whole-parameter return contract
can support only the root object-literal argument, after its own source proof,
completed veto and receipt authentication in this importer context.

The process test also refuses an open return domain, an unrun veto, a different
returned parameter, callable argument, reassigned alias and another importer
of identical dependency artifact bytes. These are
certification tests, not checker diagnostics or relaxed published typings.
No dialect stub, generated contract or finding snapshot is installed here.
