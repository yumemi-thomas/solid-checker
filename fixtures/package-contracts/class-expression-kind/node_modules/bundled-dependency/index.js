// The runtime artifact the analyzed package installs: the same bundler
// lowering, one package boundary away.
var DependencyCache = class {
  constructor(onChange) {
    this.onChange = onChange;
    onChange();
  }
};

function dependencyFunction(value) {
  return value;
}

export { DependencyCache, dependencyFunction };
