// Never executed: this fixture is generator-corpus only and no recipe runs
// against it. It exists so the analyzed package's own top-level
// `import { createEffect } from "solid-js"` has a runtime target to resolve,
// exactly as the declaration axis has `index.d.ts`.
export function createEffect() {}
