export function splitProps(props, ...keys) {
  const picked = {};
  const rest = {};
  for (const key of Object.keys(props)) {
    if (keys.includes(key)) picked[key] = props[key];
    else rest[key] = props[key];
  }
  return [picked, rest];
}
