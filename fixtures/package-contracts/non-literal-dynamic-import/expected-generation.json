{
  ".": {
    "targets": [
      "./index.js"
    ],
    "modules": [
      "index.js"
    ],
    "runtimeNotes": [
      "index.js: the module record is attested -- it names every file the analyzing program opened under this package -- and complete except for what a dynamic import() whose specifier is not statically bounded to a finite set of string literals may load at runtime, which no module graph can enumerate"
    ]
  }
}
