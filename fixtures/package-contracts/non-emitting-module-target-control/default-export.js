// `export default <expression>` binds the module's default export to an
// evaluated expression, so it emits. This is the tail of `@solidjs/h`'s
// `types/hyperscript.d.ts` in miniature, and it is why that member is not
// answered by this rule.
export default 1;
