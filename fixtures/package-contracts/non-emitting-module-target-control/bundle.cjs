// `@solid-devtools/babel-plugin@0.3.1`'s shape: a CommonJS bundle. The
// directive is a statement and it emits, so the premise never reaches the
// `module.exports` assignment.
"use strict";
var plugin = { name: "plugin" };
module.exports = { plugin };
