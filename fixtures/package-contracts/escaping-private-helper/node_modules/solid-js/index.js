// A stub runtime. Behavior for solid-js comes from the built-in Solid 2.0
// dialect model, never from these bodies; the file exists so the export names
// an arm imports resolve at all. `index.d.ts` beside it is what any type
// question resolves against.
export function For() {
  return null;
}

export function Show() {
  return null;
}
