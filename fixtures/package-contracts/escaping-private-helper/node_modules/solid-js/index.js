// A stub runtime. Behavior for solid-js comes from the bundled solid-v1
// contract keyed on `solid-js@1.9.14`, never from these bodies; the file exists
// so the export names an arm imports resolve at all. `index.d.ts` beside it is
// what any type question resolves against.
export function createSignal(value) {
  return [() => value, next => (value = next)];
}

export function For() {
  return null;
}

export function Show() {
  return null;
}
