{
  "schemaVersion": 1,
  "note": "Exact published package versions the tsc oracle compiles against. These are the versions the repository audits: `v1` matches pkg/contracts/bundled/solid-v1/solid-js.json's `package.version`, `v2` matches pkg/contracts/bundled/runtime-lock.json. A newer release must be reviewed, never silently substituted -- the oracle verifies the installed version against `expect` and refuses to run on a mismatch.",
  "dialects": {
    "v1": {
      "install": ["solid-js@1.9.14"],
      "expect": {
        "solid-js": "1.9.14"
      },
      "jsxImportSource": "solid-js",
      "audit": "pkg/contracts/bundled/solid-v1/solid-js.json"
    },
    "v2": {
      "install": [
        "solid-js@2.0.0-rc.0",
        "@solidjs/signals@2.0.0-rc.0",
        "@solidjs/web@2.0.0-rc.0"
      ],
      "expect": {
        "solid-js": "2.0.0-rc.0",
        "@solidjs/signals": "2.0.0-rc.0",
        "@solidjs/web": "2.0.0-rc.0"
      },
      "jsxImportSource": "@solidjs/web",
      "audit": "pkg/contracts/bundled/runtime-lock.json"
    }
  }
}
