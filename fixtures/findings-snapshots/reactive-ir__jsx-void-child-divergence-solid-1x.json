{
  "status": "violation",
  "findings": [
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 472,
      "end": 479,
      "fixes": 0,
      "message": "reactive accessor \"count\" is read in NestedVoidChild, inside a JSX expression the Solid compiler's execution census does not cover; whether that read is tracked cannot be proven either way, because the compiler reported no execution site for this JSX region and its silence is not evidence that the read never updates",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 592,
      "end": 599,
      "fixes": 0,
      "message": "reactive accessor \"total\" is read in RootVoidChildDependsOnTheProducer, inside a JSX expression the Solid compiler's execution census does not cover; whether that read is tracked cannot be proven either way, because the compiler reported no execution site for this JSX region and its silence is not evidence that the read never updates",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 1022,
      "end": 1025,
      "fixes": 0,
      "message": "reactive accessor \"a\" is read in RootNoscriptChild, inside a JSX expression the Solid compiler's execution census does not cover; whether that read is tracked cannot be proven either way, because the compiler reported no execution site for this JSX region and its silence is not evidence that the read never updates",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 1189,
      "end": 1195,
      "fixes": 0,
      "message": "reactive accessor \"body\" is read in NestedNoscriptOffTheFastPath, inside a JSX expression the Solid compiler's execution census does not cover; whether that read is tracked cannot be proven either way, because the compiler reported no execution site for this JSX region and its silence is not evidence that the read never updates",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 1542,
      "end": 1549,
      "fixes": 0,
      "message": "reactive accessor \"keyed\" is read in NestedKeygenChild, inside the children of an element the Solid 1.x Babel compiler treats as void; the pinned Solid compiler lowers that child into a reactive insert while that compiler deletes the element's child list, so whether this read exists at runtime — let alone whether it is tracked — cannot be certified either way",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 1655,
      "end": 1661,
      "fixes": 0,
      "message": "reactive accessor \"item\" is read in RootMenuitemChild, inside the children of an element the Solid 1.x Babel compiler treats as void; the pinned Solid compiler lowers that child into a reactive insert while that compiler deletes the element's child list, so whether this read exists at runtime — let alone whether it is tracked — cannot be certified either way",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the callback of createEffect(fn). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit."
    },
    {
      "rule": "v1/missing-owner",
      "code": "SC4001",
      "kind": "uncertifiable",
      "severity": "error",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 2377,
      "end": 2386,
      "fixes": 0,
      "message": "onCleanup is called without a reactive owner; no scope's disposal can trigger it, so this cleanup function will never run; but this call is written inside a JSX expression the Solid compiler's execution census does not cover, so whether the operation executes — and under which owner — cannot be proven",
      "hint": "Call onCleanup inside a component or computation, or create the surrounding scope with createRoot so disposal exists. Move the call to a compiler-censused position, or extend the compiler trace so its execution and owner context can be certified."
    },
    {
      "rule": "v1/missing-owner",
      "code": "SC4001",
      "kind": "violation",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-1x/App.tsx",
      "start": 2656,
      "end": 2665,
      "fixes": 0,
      "message": "onCleanup is called without a reactive owner; no scope's disposal can trigger it, so this cleanup function will never run",
      "hint": "Call onCleanup inside a component or computation, or create the surrounding scope with createRoot so disposal exists."
    }
  ]
}
