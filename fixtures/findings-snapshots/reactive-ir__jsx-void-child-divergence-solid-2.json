{
  "status": "violation",
  "findings": [
    {
      "rule": "strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 1435,
      "end": 1442,
      "fixes": 0,
      "message": "reactive accessor \"count\" is read in NestedVoidChild, inside the children of an HTML void element; the pinned Solid compiler lowers that child into a reactive insert while the compiler Solid ships deletes a void element's child list in every position, so whether this read exists at runtime — let alone whether it is tracked — cannot be certified either way",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the compute function of createEffect(compute, apply). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit. If this read executes untracked in dev, Solid warns STRICT_READ_UNTRACKED for it; this finding does not establish that it does, so confirm the read's actual execution and tracking status before relying on that warning."
    },
    {
      "rule": "strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 2562,
      "end": 2569,
      "fixes": 0,
      "message": "reactive accessor \"total\" is read in RootVoidChildDependsOnTheProducer, inside a JSX expression the Solid compiler's execution census does not cover; whether that read is tracked cannot be proven either way, because the compiler reported no execution site for this JSX region and its silence is not evidence that the read never updates",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the compute function of createEffect(compute, apply). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit. If this read executes untracked in dev, Solid warns STRICT_READ_UNTRACKED for it; this finding does not establish that it does, so confirm the read's actual execution and tracking status before relying on that warning."
    },
    {
      "rule": "strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 4532,
      "end": 4535,
      "fixes": 0,
      "message": "reactive accessor \"a\" is read in RootNoscriptChild, inside the children of a `<noscript>`; the pinned Solid compiler lowers that child into a reactive insert while the compiler Solid ships never lowers `<noscript>` children in any position, so whether this read exists at runtime — let alone whether it is tracked — cannot be certified either way",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the compute function of createEffect(compute, apply). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit. If this read executes untracked in dev, Solid warns STRICT_READ_UNTRACKED for it; this finding does not establish that it does, so confirm the read's actual execution and tracking status before relying on that warning."
    },
    {
      "rule": "strict-read-untracked",
      "code": "SC1001",
      "kind": "uncertifiable",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 5466,
      "end": 5472,
      "fixes": 0,
      "message": "reactive accessor \"body\" is read in NestedNoscriptOffTheFastPath, inside the children of a `<noscript>`; the pinned Solid compiler lowers that child into a reactive insert while the compiler Solid ships never lowers `<noscript>` children in any position, so whether this read exists at runtime — let alone whether it is tracked — cannot be certified either way",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the compute function of createEffect(compute, apply). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit. If this read executes untracked in dev, Solid warns STRICT_READ_UNTRACKED for it; this finding does not establish that it does, so confirm the read's actual execution and tracking status before relying on that warning."
    },
    {
      "rule": "missing-owner",
      "code": "SC4001",
      "kind": "uncertifiable",
      "severity": "error",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 8768,
      "end": 8777,
      "fixes": 0,
      "message": "onCleanup is called without a reactive owner; no scope's disposal can trigger it, so this cleanup function will never run; but this call is written inside the children of an HTML void element, and the compiler Solid ships deletes a void element's child list in every position while the pinned Solid compiler lowers that child into a reactive insert that owns it — so whether this call runs at all, and under which owner, depends on which compiler builds this project",
      "hint": "Call onCleanup inside a component or computation, or create the surrounding scope with createRoot so disposal exists. For one-time setup with teardown, use onSettled with a returned cleanup in a component. Move the call out of that element's children: while it sits there, no compiler fact decides its owner, and the two candidate compilers disagree about whether it executes."
    },
    {
      "rule": "missing-owner",
      "code": "SC4001",
      "kind": "violation",
      "severity": "warning",
      "path": "fixtures/reactive-ir/jsx-void-child-divergence-solid-2/App.tsx",
      "start": 9437,
      "end": 9446,
      "fixes": 0,
      "message": "onCleanup is called without a reactive owner; no scope's disposal can trigger it, so this cleanup function will never run",
      "hint": "Call onCleanup inside a component or computation, or create the surrounding scope with createRoot so disposal exists. For one-time setup with teardown, use onSettled with a returned cleanup in a component."
    }
  ]
}
