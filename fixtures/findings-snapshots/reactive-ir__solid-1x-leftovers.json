{
  "status": "violation",
  "findings": [
    {
      "rule": "strict-read-untracked",
      "code": "SC1001",
      "kind": "violation",
      "severity": "warning",
      "path": "fixtures/reactive-ir/solid-1x-leftovers/App.tsx",
      "start": 485,
      "end": 492,
      "fixes": 0,
      "message": "reactive accessor \"count\" is read directly in rendering function, which does not track; the read sees the current value once and never updates when \"count\" changes",
      "hint": "Move the read into a tracking scope: JSX, a createMemo, or the compute function of createEffect(compute, apply). If a one-time snapshot is intended, wrap the read in untrack() to make that explicit. Solid warns STRICT_READ_UNTRACKED here in dev."
    },
    {
      "rule": "reactive-write-in-owned-scope",
      "code": "SC2001",
      "kind": "violation",
      "severity": "error",
      "path": "fixtures/reactive-ir/solid-1x-leftovers/App.tsx",
      "start": 511,
      "end": 522,
      "fixes": 0,
      "message": "accessor setter \"setCount\" is called inside owned scope Migrated; writes under a children-capable owner create feedback loops in the reactive graph, and Solid throws REACTIVE_WRITE_IN_OWNED_SCOPE here in dev",
      "hint": "Derive the value instead of writing it back: replace compute-then-set with a createMemo. If the write is genuinely imperative, move it to an event handler, an action, the apply function of createEffect(compute, apply), or a leaf scope (onSettled, createTrackedEffect). Wrapping the write in untrack() does not help: the guard keys on the owner, not on tracking. For an internal reactive source only, opt in with { ownedWrite: true } in that source's creation options."
    },
    {
      "rule": "reactive-write-in-owned-scope",
      "code": "SC2001",
      "kind": "violation",
      "severity": "error",
      "path": "fixtures/reactive-ir/solid-1x-leftovers/App.tsx",
      "start": 539,
      "end": 550,
      "fixes": 0,
      "message": "accessor setter \"setCount\" is called inside owned scope Migrated; writes under a children-capable owner create feedback loops in the reactive graph, and Solid throws REACTIVE_WRITE_IN_OWNED_SCOPE here in dev",
      "hint": "Derive the value instead of writing it back: replace compute-then-set with a createMemo. If the write is genuinely imperative, move it to an event handler, an action, the apply function of createEffect(compute, apply), or a leaf scope (onSettled, createTrackedEffect). Wrapping the write in untrack() does not help: the guard keys on the owner, not on tracking. For an internal reactive source only, opt in with { ownedWrite: true } in that source's creation options."
    }
  ]
}
