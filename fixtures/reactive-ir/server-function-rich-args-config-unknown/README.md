# server-function-rich-args-config-unknown

Pins the third SC7007 outcome. The exact
`configureServerFunctionsClient` declaration is resolved, but its options
object is runtime-supplied and `serializeArgs` is optional. A rich argument is
therefore uncertifiable rather than a proven default-transport violation or a
silently accepted configured call.
