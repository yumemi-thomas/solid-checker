// A transparent wrapper: whatever you pass at argument 0 comes back. Its
// contract states exactly that -- `returns: {kind: "argument", parameter: 0}` --
// which is the only premise under which the consumer looks *through* the call
// at what the argument itself returns.
export declare function identity<T>(value: T): T;
