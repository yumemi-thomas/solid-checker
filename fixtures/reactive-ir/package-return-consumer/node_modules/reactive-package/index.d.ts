export declare function createCount(): () => number;
// Deliberately the *same* signature and the same contract summary as
// `createCount`. This export exists so a demand-scoping change has a control:
// two bindings whose contracts are byte-identical, used in different
// positions. Any difference in what is reported about them is attributable to
// the use position alone.
export declare function createLabel(): () => number;
