export function createSubRoot(fn) {
  return fn(() => {});
}

export function createSingletonRoot(factory) {
  let value;
  return () => (value ??= factory(() => {}));
}

export function createRootPool(factory) {
  return arg => factory(() => arg, () => true, () => {});
}
